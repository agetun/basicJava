package homework.lesson10HW;

public class Task_6 {

    //Напишите тесты для проверки метода isEven(int number).
    //Упростите работу метода, код внутри может быть написан в одну строку.

    public boolean isEven(int number) {
        return (number % 2 == 0);
    }
}
